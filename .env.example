TELEGRAM_BOT_TOKEN=
OWNER_TELEGRAM_USER_ID=
DATABASE_URL=postgresql+psycopg://finbot:finbot-dev-only@db:5432/finbot
DEFAULT_LOCALE=ru_RU
DEFAULT_TIMEZONE=Europe/Moscow
DEFAULT_CURRENCY=RUB
LOG_LEVEL=INFO
RECURRING_TICK_INTERVAL_SECONDS=30
API_PUBLISHED_PORT=8080
MINIAPP_PUBLIC_URL=
MINIAPP_HTTPS_BIND=0.0.0.0
MINIAPP_HTTPS_PORT=443
HTTP_SECURITY_KEY=
# Independent base64url-encoded 32-byte key for import fingerprints and deduplication.
BANK_IMPORT_SECURITY_KEY=
# Optional explicit `/ai <text>` flow. Disabled means zero model calls.
LOCAL_AI_ENABLED=false
# LOCAL_AI_ENDPOINT=http://127.0.0.1:11434
# LOCAL_AI_MODEL=qwen3:4b
# Production TLS files: secrets/miniapp_tls_fullchain.pem and
# secrets/miniapp_tls_private_key.pem (host uid:gid 101:101, private key mode 0400).
